{
  "name": "server",
  "version": "1.0.0",
  "description": "server for wertc tech",
  "license": "ISC",
  "author": "Koderkup",
  "type": "commonjs",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": " npm run build",
    "start": "concurrently --kill-others \"npm run watch\" \"nodemon .\""
  },
  "devDependencies": {
    "tslint": "^5.20.1",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.10"
  }
}
